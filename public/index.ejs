<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <style>
        html,
        body {
            width: 100%;
            height: 100%;
        }

        html,
        body,
        div {
            margin: 0;
            padding: 0;
        }

        .main {
          margin-top: 20px;
            display: flex;
            justify-content: space-around;
        }

        .box {
            width: 300px;
            height: 300px;
            border: 1px solid #000;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            transition: all .2s linear;
        }

        .pic {
            width: 200px;
        }

        .box:hover{
            transform: scale(1.05);
        }
    </style>
</head>

<body>
    <div class="main">
        <% for (var i=0;i<list.length;i++){ %>
            <div class="box">
                <img class="pic" src="<%=list[i].showUrl%>" alt="">
                <div>
                    <a href="<%= list[i].fullUrl %>"><%= list[i].fullUrl %></a>
                </div>
            </div>
            <% } %>
    </div>
</body>

</html>